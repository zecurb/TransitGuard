-- TransitGuard PostgreSQL persistence baseline.
--
-- SQLx records this migration in its migration-history table. Domain tables
-- will be introduced through subsequent reviewed migrations.

SELECT 1;
